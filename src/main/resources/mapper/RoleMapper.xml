<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.how2java.mapper.RoleMapper">
  <resultMap id="BaseResultMap" type="com.how2java.pojo.Role">
    <id column="id" jdbcType="BIGINT" property="id" />
    <result column="name" jdbcType="VARCHAR" property="name" />
    <result column="desc_" jdbcType="VARCHAR" property="desc_" />
  </resultMap>


  <select id="getRoleByUser"  parameterType="com.how2java.pojo.User" resultType="com.how2java.pojo.Role">
    select r.id,r.name from user u
                                      left join user_role ru
                                                on u.id = ru.uid left
                                        join role r
                                             on r.id = ru.rid

    where u.id =#{id}
  </select>

  <select id="getAll"  resultType="com.how2java.pojo.Role">
    select * from role

  </select>


  <delete id="deleteById" parameterType="java.lang.Long">
    delete from role
    where id = #{id}
  </delete>

  <insert id="insert" parameterType="com.how2java.pojo.Role">
    insert into role (name, desc_)
    values (#{name}, #{desc_})
  </insert>

  <select id="getById"  resultType="com.how2java.pojo.Role">
    select * from role where id=#{id}

  </select>

  <update id="update" parameterType="com.how2java.pojo.Role">
    update role
    <set>
      <if test="name != null">
        name = #{name},
      </if>
      <if test="desc_ != null">
        desc_ = #{desc_},
      </if>
    </set>
    where id = #{id}
  </update>

  <sql id="Example_Where_Clause">
    <where>
      <foreach collection="oredCriteria" item="criteria" separator="or">
        <if test="criteria.valid">
          <trim prefix="(" prefixOverrides="and" suffix=")">
            <foreach collection="criteria.criteria" item="criterion">
              <choose>
                <when test="criterion.noValue">
                  and ${criterion.condition}
                </when>
                <when test="criterion.singleValue">
                  and ${criterion.condition} #{criterion.value}
                </when>
                <when test="criterion.betweenValue">
                  and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                </when>
                <when test="criterion.listValue">
                  and ${criterion.condition}
                  <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                    #{listItem}
                  </foreach>
                </when>
              </choose>
            </foreach>
          </trim>
        </if>
      </foreach>
    </where>
  </sql>
  <sql id="Base_Column_List">
    id, name, desc_
  </sql>
  <select id="selectByExample" parameterType="com.how2java.pojo.RoleExample" resultMap="BaseResultMap">
    select
    <if test="distinct">
      distinct
    </if>
    'false' as QUERYID,
    <include refid="Base_Column_List" />
    from role
    <if test="_parameter != null">
      <include refid="Example_Where_Clause" />
    </if>
    <if test="orderByClause != null">
      order by ${orderByClause}
    </if>
  </select>
  <select id="selectByPrimaryKey" parameterType="java.lang.Long" resultMap="BaseResultMap">
    select 
    <include refid="Base_Column_List" />
    from role
    where id = #{id,jdbcType=BIGINT}
  </select>


  <insert id="insertSelective" keyColumn="id" keyProperty="id" parameterType="com.how2java.pojo.Role" useGeneratedKeys="true">
    insert into role
    <trim prefix="(" suffix=")" suffixOverrides=",">
      <if test="name != null">
        name,
      </if>
      <if test="desc_ != null">
        desc_,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides=",">
      <if test="name != null">
        #{name,jdbcType=VARCHAR},
      </if>
      <if test="desc_ != null">
        #{desc_,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>

  <update id="updateByPrimaryKey" parameterType="com.how2java.pojo.Role">
    update role
    set name = #{name,jdbcType=VARCHAR},
      desc_ = #{desc_,jdbcType=VARCHAR}
    where id = #{id,jdbcType=BIGINT}
  </update>
</mapper>